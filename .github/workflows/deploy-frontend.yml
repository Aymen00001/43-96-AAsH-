name: Build & Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'Frontend/**'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'Frontend/package-lock.json'

      - name: Install dependencies
        working-directory: Frontend
        run: npm ci

      - name: Run linter
        working-directory: Frontend
        run: npm run lint
        continue-on-error: true

      - name: Build project
        working-directory: Frontend
        run: npm run build

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: './Frontend/dist/'
          server-dir: '/statisticsV2.makseb.fr/'
          state-name: '.ftp-deploy-sync-state.json'
          dry-run: false

      - name: Deployment Success
        if: success()
        run: echo "✅ Frontend deployed successfully to FTP"

      - name: Deployment Failed
        if: failure()
        run: echo "❌ Deployment failed"
name: Build & Deploy Frontend

on:
  push:
    branches:
      - main
    paths:
      - 'Frontend/**'
      - '.github/workflows/deploy-frontend.yml'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: 'Frontend/package-lock.json'

      - name: Install dependencies
        working-directory: Frontend
        run: npm ci

      - name: Run linter
        working-directory: Frontend
        run: npm run lint
        continue-on-error: true

      - name: Build project
        working-directory: Frontend
        run: npm run build

      - name: Deploy to FTP
        uses: SamKirkland/FTP-Deploy-Action@4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          local-dir: './Frontend/dist/'
          server-dir: '/statisticsV2.makseb.fr/'
          state-name: '.ftp-deploy-sync-state.json'
          dry-run: false

      - name: Deployment Success
        if: success()
        run: echo "✅ Frontend deployed successfully to FTP"

      - name: Deployment Failed
        if: failure()
        run: echo "❌ Deployment failed"
